#include<stdio.h>
#include<string.h>
int main()
{
    char a[60];
    int x,y,z,i;
    scanf("%d %d",&x,&y);
    scanf("%s",a);
    z=strlen(a);
    for(i=1;i<=z;i++)
    {
        if(y==i)
            a[i-1]=a[i-1]+32;
    }
    printf("%s\n",a);
}

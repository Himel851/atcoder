#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,k,a[100005],count=0;
    cin>>n>>k;
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
        if(a[i]>=k)
            count++;
    }
    cout<<count<<endl;
}
